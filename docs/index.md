# 项目文档（next-blog）

这是项目的开发与维护文档目录。文档分为：

- 项目概览与快速开始（本页）
- `scripts` 目录脚本使用说明（见 `scripts.md`）

快速开始：

```bash
# 安装依赖
npm install

# 启动开发环境（会同时启动 watch 脚本和 Next 开发服务器）
npm run dev
```

更多细节请参阅 `scripts.md`，以及项目根目录下的 `package.json` 中定义的 `scripts` 字段。